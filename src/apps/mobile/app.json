{
  "expo": {
    "name": "Cal&der",
    "slug": "calder",
    "version": "0.1.0",
    "orientation": "default",
    "userInterfaceStyle": "automatic",
    "newArchEnabled": true,
    "//scheme": "Universal links carry invite tokens straight into the app when it is installed (Architecture.md \u00a77.1, \u00a713.9).",
    "scheme": "calder",
    "ios": {
      "supportsTablet": true,
      "bundleIdentifier": "app.calder.mobile"
    },
    "android": {
      "package": "app.calder.mobile",
      "edgeToEdgeEnabled": true
    },
    "web": {
      "bundler": "metro",
      "output": "static"
    },
    "plugins": [
      "expo-router",
      "expo-screen-orientation",
      [
        "expo-image-picker",
        {
          "photosPermission": "Cal&der uses your photos so you can put a cover picture on a calendar.",
          "cameraPermission": false,
          "microphonePermission": false
        }
      ]
    ],
    "experiments": {
      "typedRoutes": true
    },
    "//plugins.expo-image-picker": "Without this the Info.plist has no NSPhotoLibraryUsageDescription, so iOS shows no Photos row for the app in Settings and a real build crashes on the first request.",
    "//name": "Display name only. The ampersand is illegal in bundle ids, npm package names, URL schemes and domains, so every machine-readable form is 'calder'."
  }
}
